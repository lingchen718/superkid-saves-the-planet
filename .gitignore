# Python virtualenv (memory [PROJECTS]: project includes venv/)
venv/
.venv/
__pycache__/
*.pyc

# Build / packaging artifacts
build/
.pygbag_*

# OS / IDE
.DS_Store
.idea/

# Abandoned APK / Buildozer artifacts (memory [PROJECTS] blocker: APK path failed)
# Pygbag's web bundle itself (.apk as game-data zip) is NOT in build/ — it's
# intentionally tracked and shipped as part of the web bundle.
.buildozer/
bin/

# Local environment
venv/
